/// Central place for contact details used in Settings, the FAQ and the
/// Impressum — update [contactEmail] here once a real inbox exists instead
/// of hunting through the UI for hardcoded addresses.
class AppInfo {
  AppInfo._();

  /// Placeholder until a dedicated inbox exists.
  static const contactEmail = 'kontakt@samepace.app';
}

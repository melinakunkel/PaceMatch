/// Central place for contact details used in Settings, the FAQ and the
/// Impressum/Datenschutz — change them here instead of hunting through the
/// UI for hardcoded values.
class AppInfo {
  AppInfo._();

  /// Where every support, feedback and privacy request goes.
  static const contactEmail = 'samepace@outlook.de';

  /// The person operating the app (Impressum, Verantwortlicher).
  static const operatorName = 'Melina Kunkel';
}
